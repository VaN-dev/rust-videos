{% extends '::layout.html.twig' %}

{% block main %}
    <div class="main">

        <div class="container">
            <h2>{{ category.name }}</h2>

            <div class="row">
                {% for video in videos %}
                    <div class="col-md-4">
                        {% include '@App/Video/widget-simple.html.twig' %}
                    </div>
                {% endfor %}
            </div>
        </div>

    </div>
{% endblock %}