{% extends '::layout.html.twig' %}

{% block main %}


    <div class="main">

        <div class="container">
            <h1>Register</h1>

            <div class="row">
                {{ form_start(form) }}
                    {{ form_widget(form) }}

                    <button type="submit" class="btn btn-default">Register</button>
                {{ form_end(form) }}
            </div>
        </div>

    </div>
{% endblock %}
