{% extends '::layout.html.twig' %}

{% block main %}
    <div class="main">

        <div class="container">
            <div class="row">
                {% for video in videos %}
                    <div class="col-md-4">
                        {% include '@App/Video/widget-simple.html.twig' %}
                    </div>
                {% endfor %}
            </div>

            <a href="{{ path('app.video.insert') }}" class="btn btn-default">Add a video &raquo;</a>
        </div>

    </div>
{% endblock %}